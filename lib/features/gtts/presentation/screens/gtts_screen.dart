import 'package:flutter/material.dart';

class GttsScreen extends StatelessWidget {
  const GttsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("GTTs"));
  }
}
