import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color.fromARGB(255, 0, 0, 0);
  static const secondary = Color(0xFF00C853);

  static const background = Colors.white;
  static const textPrimary = Colors.black;
  static const textSecondary = Colors.grey;
  static var grey = Colors.grey;
  static var lightGrey = Colors.grey[300];
  static var textField = Colors.grey[100];

  static const profit = Colors.green;
  static const loss = Colors.red;
}
